import { elapsedTime } from '../../../helpers/elapsed-time';
import { module, test } from 'qunit';

module('Unit | Helper | elapsed time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = elapsedTime(42);
  assert.ok(result);
});
